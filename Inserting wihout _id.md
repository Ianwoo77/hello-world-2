# Inserting wihout `_id`

In all the examples we showed up till now, we explicitly added a PK `_id`field, however, in -- learned that while creating a new document, Mongodb verifies the presence and quniquness of a given PK and, if the PK is not already in present, the dbs *autogenerates it* andd adds it into the document, and the following is a snippet from the mongodb shell where an `insert`command is executed -- the `insertOne`is trying to push a new movie into the collection, but the document doesn’t have an `_id`field. fore:

```js
db.new_movies.insert({'title': 'thelma'}) // WriteResult ({nInserted: 1})
```

In the previous snppet, the reslt shows that the coument exists in the collection and an autogenerted `_id`filed is added to the document -- The augogenerted primaryis derived from the `Ovbject Id`ctor and its gloally unique.

```js
db.new_movies.insertMany([
    {"_id" : 9, "title" : "movie_1"},
    {"_id" : 10, "title" : "movie_2"},
    {"title" : "movie_3"},
    {"_id" : 8, "title" : "movie_4"},
])
```

And the output of the query indicates that the query was successful, and the `insertedids`field shows tht all documents expect the 3rd were inserted with the given keys nd the 3rd document get an autogenerated pk. PKs are the ones that can uniquely identify a record. Mdb’s autogeneated keys are usual in terms of uniqueness,  Fore, in dbs, should always try to use the Pks that already in the dbos, fore, the user’s dataset, the `email_address`filed is the best example of a PK, however, in case of movies, there is no field that cn be unique, so the purpose, use authgenerted pks.

#### Deleting Documents

To delete one or more documents from a collection, we have to use one of the various delete functions provided by Mdb. Using `deletetOne()`-- is used to delete a single document from a collection, accepts document representing a query condition.Ad the method opertion was confiired - as the method deletes only one document, the value of `deletecCount`always one, if the the given query condition query condition matchs more then one document in the collection, only the firset document will bel deleted. Fore;

```js
db.new_movies.deleteOne("{_id:2}")
db.new_movies.deleteOne({title:{$regex:'^movie'}})
db.new_movies.find()
```

#### Deleteing Multipel documents using `deleteMany()`

To delete multipel documents tht matche the given criteria, you can execute the `deleteOne`function multiple times, however, in that case, each document will be deleted in separate command, which an slowdown the performance,  And the `documentMany()` function must provide with a query condition, and all the documents that matche the given query will be removed -- fore:

```js
db.new_movies.deleteMany({'title': {$regex:'^movie'}})
```

The `deleteMany()`function must be provided with a query condition, and all the documents that match the given query will be removed - like:

```CS
db.new_movies.deleteMany({title:{$regex: '^movie'}})
```

And the the following exmple, both of the doucments hve been given an empty query document just lke:

```js
db.new_movies.deleteOne({})
db.new_movies.deleteMany({})
```

the `deleteOne()`will delete the document that is found first, however, the `deleteMany()`will delete all the documents in the collection. Lao in the mdb, a non-existent filed is considered to be `null`so the given condition will match *all* of the documents in the collection just like:

```js
db.new_movies.deleteMany({'non_existent-field': null})
```

##### Deleting using `findOneAndDelete()`

Apart from the two delete methods we awa - there is another function named `findOneAndDelete()`- finds and deletes one document from the collection -- although it behavies to the `deleteOne()`it provides a few more option:

1. It finds one and deletes it
2. if more than one is found, only the first will be deleted
3. Once doeleted, returns the delteed as a response
4. in the case of multiple document matchs, the `sort`option can be used to influence whcih docment get deleted.

```js
db.new_movies.findOneAndDelete({_id:3})
```

Fore, if add some documents like:

```js
db.new_movies.insertMany([
  { "_id" : 11, "title" : "movie_11" },
  { "_id" : 12, "title" : "movie_12" },
  { "_id" : 13, "title" : "movie_13" },
  { "_id" : 14, "title" : "movie_14" },
  { "_id" : 15, "title" : "series_15" }
])
```

```js
db.new_movies.findOneAndDelete(
    {title:{'$regex':'^movie'}},
    {sort: {_id:-1}, projection: {_id:0, title:1}}
)
db.new_movies.find()
```

Also can see, this delete function always returns the deleted document in the respone, 

## Being puzzled about channel size -- 

When created a channel using the `make`built-in function, the channel can be either unbuffered or buffered, related to this topic, two mistakes happen farily frequently -- being confused when to use one or other, and if used buffered channel, what size use - fore, remember tht the core concepts, an unbuffered channel is a channel without any capacity, it can be crated either omitting the sie or providing 0 as a size just like:

```go
ch1 := make(chan int)
ch2 := make(chan int, 0)
```

Using an unbuffered channel, the sender will block until the receivers data from the channel. Conversely, a buffered channel ahs a capacity, and it must be created with a ize greater than equal to 1.

```go
ch3 := make(chan int, 1)
```

When a buffered channel, a sneder can send messages while the channel isn’t full. once the channel is full, it will block until a receiver goroutine receives a message fore -- just like:

```go
ch3 := make(chan int, 1)
ch3 <-1
ch3 <-2 // block
```

Channels are concurrecny abstraction to enable communication between two channel types, channels are concurency bstracton to enable communication among goroutines - but what about sync -- Sync means that we can guarantee that multiple goroutines will be in a known state at aome pont -- fore, a mutex provides sync because it ensures ahant only one gorouine can be n a critieral section at the same time.

If we need sync, we must use unbufferred channels, unbuffered channels may also be easier to reason about, -- buffered channels can lead to obscrure deadlocks about that leads to deadlocks that ould be immediately apparent with unbuffered channels.

And there are other cases where unbuffered are preferable, fore, in the case of notification channel where the notifiations is handled via a channel closure -- using a buffered channel wouldn’t bring any benefits.

- While using a worker pooling -- like pattern, meaning spinning a fixed number of goroutines that need to send data to a shared channel, in that ase, can tie the channel size to the humber of good goroutine created.
- Fore, spinning a fixed number of goroutines that need to send data to a shared channel in that ase, can tie the channel size to the number of goroutines crated.